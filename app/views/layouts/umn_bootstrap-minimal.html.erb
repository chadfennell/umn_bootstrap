<!DOCTYPE HTML>
<html>
  <head>
    <%= render :partial => 'layouts/umn_bootstrap/common/head' %>
    <style>
      body {
        padding-top: 60px; /* 60px to make the container go all the way to the bottom of the topbar */
      }
      a.brand img {
        margin-top:-3px;
        margin-right:5px;
        opacity:.5;
      }
      a.brand:hover img {
        opacity:1;
      }
    </style>
  </head>
  <body>

    <div class="navbar navbar-inverse navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <button type="button" class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="brand" href="#"><%= image_tag "blockm_white.png", :style => "" %> <%= render :partial => 'shared/umn_bootstrap/title' %></a>
          <div class="nav-collapse collapse">
            <!-- For this layout, your navigation partial should have this structure:
            <ul class="nav">
              <li class="active"><a href="#">Home</a></li>
              <li><a href="#about">About</a></li>
              <li><a href="#contact">Contact</a></li>
            </ul>
            -->
          </div>
        </div>
      </div>
    </div>

    <div class="container">
      <%= yield %>
    </div>

    <%= render_if_exists :partial => 'shared/umn_bootstrap/analytics' %>
  </body>
</html>


